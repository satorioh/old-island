<!--components/navi/navi.wxml-->
<view class="container">
  <image class="icon" src="{{last ? disLeftSrc : leftSrc}}" bind:tap="leftTap"/>
  <text class="title">{{title}}</text>
  <image class="icon" src="{{first ? disRightSrc : rightSrc}}" bind:tap="rightTap"/>
</view>
